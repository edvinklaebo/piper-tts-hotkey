# Piper TTS Hotkey

A lightweight Windows Python script that reads your clipboard via a hotkey using Piper TTS.

## Requirements

- Python 3.14+
- Piper TTS (https://github.com/rhasspy/piper)
- A Piper voice model (`.onnx`)

Install dependencies:

```bash
pip install -r requirements.txt
